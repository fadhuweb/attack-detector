# Code is edited on Windows and run on Linux. Anything the target executes or
# parses must keep LF endings: a shell script with CRLF fails on Ubuntu with
# "bad interpreter: /usr/bin/env bash^M".
* text=auto eol=lf
*.ps1 text eol=crlf
*.log -text
